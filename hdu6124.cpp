#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long a;
    int T;
    while(cin >> T)
    {
        while(T--)
        {
            cin >> a ;
            cout << (a + 1)/ 2 + 1 << endl;
        }
    }
}
