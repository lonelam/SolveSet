#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

struct edge
{
    int id, to, cost;
};
int n, q, s;
vector<edge> es;
int a[maxn], b[maxn], w[maxn];

vector<int> G[maxn];

int root;
int vs[maxn * 2 - 1];
int depth[maxn * 2 - 1];
int id[maxn];
int es[]
void dfs(int u, int p, int d)
{

}
