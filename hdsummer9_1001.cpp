#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
map<int, ll> dp;
char cmd[100];
int u;
int n, m;
int main()
{
    while(scanf("%d%d", &n, &m)!=EOF)
    {
        dp.clear();
        for (int i = 0; i < m; i++)
        {
            scanf("%s%d", &cmd, &u);
        }
    }
}
