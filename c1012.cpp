#include<bits/stdc++.h>
using namespace std;
const int maxn = 51;
int dp1[maxn];
int dp[maxn][maxn];
bool ans[maxn][maxn][maxn];
int a[maxn];

struct res
{
    set<int> in;
    int sum;
};
void solve(int n)
{
    set<res> sr;
    for(int i = 1; i < n; i++)
    {
        for(res & s : sr)
        {

        }
    }
}

int main()
{
    int t, n, q;
    scanf("%d", & t)
    while(t--)
    {
        scanf("%d",&n);
        for(int i = 1; i <= n; i++)
        {
            scanf("%d", a+ i);
        }
        scanf("%d", &q);

        for(int i = 1; i <= n; i++)
        {

        }
    }
}
